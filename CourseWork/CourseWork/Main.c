#include "size.h"//підключаємо глобальний хедер з розмірами
#include "Menu.h"

int A[P][M][N];//обявлаємо статичний масив
int B[Nvect]; // обявляємо статичний вектор

int main()
{
	system("color 2");//змінюємо колір тексту програми

	srand(time(0));//для коректної роботи функції рандом
	
	zastavka();//заставка перед початком програми
	menu();//викликаємо меню

	_getch();//затримка після закінчення програми
	return 0;
}
